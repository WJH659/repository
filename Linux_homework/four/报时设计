我们可以在配置文件中添加一个命令，让它在启动时自动运行 cowsay 来报告当前时间。
实现步骤：
一、下载cowsay
apt update
apt install cowsay
二、添加环境变量
export PATH=$PATH:/usr/games
三、实现
# 将报时命令追加到 .bashrc 文件末尾
echo 'cowsay "The time is $(date)"' >> ~/.bashrc
# 立即加载更改后的 .bashrc 文件，使设置在当前会话中生效
source ~/.bashrc
